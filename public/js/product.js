const qs = new URLSearchParams(location.search); const id = Number(qs.get('id')); async function load(){ const res = await fetch('/api/products/'+id); if(!res.ok){ document.getElementById('product-el').innerText = 'Product not found'; return; } const p = await res.json(); document.getElementById('product-el').innerHTML = `<div style="display:flex;gap:16px;flex-wrap:wrap"><div style="flex:1;min-width:220px" class="product-image">${p.img? `<img src="${p.img}" style="max-width:100%">`:'<svg width="260" height="180" viewBox="0 0 260 180"><rect width="260" height="180" rx="12" fill="#fff0fb"/></svg>'}</div><div style="flex:1;min-width:260px"><h2>${p.title}</h2><p class="product-desc">${p.desc||''}</p><div style="font-weight:700;font-size:20px">₹${p.price}</div><div style="margin-top:12px"><button class="btn primary" id="add">Add to cart</button> <a class="btn ghost" href="/pages/cart.html">Go to cart</a></div></div></div>`; document.getElementById('add').onclick = ()=>{ const cart = JSON.parse(localStorage.getItem('je_cart')||'{}'); cart[id]=(cart[id]||0)+1; localStorage.setItem('je_cart', JSON.stringify(cart)); alert('Added'); } } load();